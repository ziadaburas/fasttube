# 🚀 YouTube Downloader API - ابدأ من هنا

## 📦 ما تم إنشاؤه لك

تم إنشاء **API متكامل ومتقدم** لتحميل الفيديوهات من YouTube يدعم:

### ✨ المميزات الرئيسية
- ✅ تحميل جميع أنواع الفيديوهات (عادية، محمية عمرياً، بث مباشر)
- ✅ تحميل قوائم التشغيل الكاملة
- ✅ تحميل الصوت فقط بصيغة MP3
- ✅ اختيار الجودة (4K, 1080p, 720p, وأكثر)
- ✅ تجاوز القيود الجغرافية والعمرية
- ✅ متابعة تقدم التحميل في الوقت الفعلي
- ✅ دعم التحميل غير المتزامن (async)
- ✅ واجهة RESTful API سهلة الاستخدام

---

## 📁 الملفات المُنشأة

### 1. الملفات الأساسية
| الملف | الوصف |
|------|-------|
| `youtube_downloader_api.py` | 🎯 **الملف الرئيسي** - API كامل جاهز للتشغيل |
| `requirements.txt` | 📦 المكتبات المطلوبة |
| `example_usage.py` | 💻 أمثلة تفاعلية للاستخدام |

### 2. الملفات التوثيقية
| الملف | المحتوى |
|------|---------|
| `README_AR.md` | 📖 **توثيق شامل** بالعربية - ابدأ هنا! |
| `QUICK_START.md` | ⚡ دليل البدء السريع |
| `ADVANCED_FEATURES.md` | 🔧 المميزات المتقدمة والخيارات |
| `TROUBLESHOOTING.md` | 🔍 حل المشاكل الشائعة |
| `API_EXAMPLES.md` | 📚 أمثلة شاملة (Python, cURL, JS, PHP) |

### 3. ملفات Docker
| الملف | الوصف |
|------|-------|
| `Dockerfile` | 🐳 بناء صورة Docker |
| `docker-compose.yml` | 🚢 تشغيل الحاوية |
| `.dockerignore` | 📝 استثناءات Docker |

### 4. ملفات الاختبار
| الملف | الوصف |
|------|-------|
| `test_api.sh` | 🧪 اختبار API بأوامر cURL |
| `.env.example` | ⚙️ مثال لمتغيرات البيئة |

---

## 🎯 البدء السريع (3 خطوات)

### الخطوة 1: تثبيت المتطلبات

```bash
# تثبيت المكتبات
pip install -r requirements.txt

# تثبيت ffmpeg (ضروري!)
# على Ubuntu/Debian:
sudo apt-get install ffmpeg

# على macOS:
brew install ffmpeg
```

### الخطوة 2: تشغيل API

```bash
python youtube_downloader_api.py
```

سيعمل API على: **http://localhost:5000**

### الخطوة 3: اختبار API

#### أ) استخدام Python:
```bash
python example_usage.py
```

#### ب) استخدام cURL:
```bash
./test_api.sh
```

#### ج) استخدام المتصفح:
افتح: http://localhost:5000

---

## 💡 أمثلة سريعة

### مثال 1: تحميل فيديو واحد

```python
import requests

response = requests.post(
    "http://localhost:5000/api/download",
    json={
        "url": "https://youtube.com/watch?v=VIDEO_ID",
        "format_type": "best",
        "async": True
    }
)

print(response.json())
```

### مثال 2: تحميل صوت فقط (MP3)

```python
response = requests.post(
    "http://localhost:5000/api/download",
    json={
        "url": "https://youtube.com/watch?v=VIDEO_ID",
        "format_type": "audio",
        "async": True
    }
)
```

### مثال 3: الحصول على معلومات الفيديو

```python
response = requests.post(
    "http://localhost:5000/api/info",
    json={"url": "https://youtube.com/watch?v=VIDEO_ID"}
)

info = response.json()
print(f"العنوان: {info['title']}")
print(f"المدة: {info['duration']} ثانية")
```

---

## 🌐 نقاط النهاية (API Endpoints)

| النهاية | الوصف |
|--------|-------|
| `POST /api/info` | الحصول على معلومات الفيديو |
| `POST /api/download` | تحميل فيديو |
| `POST /api/download/playlist` | تحميل قائمة تشغيل |
| `GET /api/status/{id}` | حالة التحميل |
| `POST /api/formats` | الصيغ المتاحة |
| `GET /api/health` | فحص صحة API |

---

## 📖 خريطة التعلم

### 1️⃣ مبتدئ
- ✅ ابدأ بـ `QUICK_START.md`
- ✅ جرّب `example_usage.py`
- ✅ اقرأ الأمثلة في `API_EXAMPLES.md`

### 2️⃣ متوسط
- ✅ استكشف `README_AR.md` الكامل
- ✅ تعلم المميزات من `ADVANCED_FEATURES.md`
- ✅ خصص API حسب احتياجاتك

### 3️⃣ متقدم
- ✅ راجع `TROUBLESHOOTING.md` للمشاكل
- ✅ أضف ميزات جديدة
- ✅ استخدم Docker للنشر

---

## 🔥 حالات استخدام شائعة

### 1. تحميل فيديوهات محمية عمرياً

```python
# احصل على ملف cookies من متصفحك أولاً
# ثم عدّل في youtube_downloader_api.py:

def get_cookies_for_age_restricted():
    return "/path/to/your/cookies.txt"
```

### 2. تحميل قناة كاملة

```python
response = requests.post(
    "http://localhost:5000/api/download/playlist",
    json={
        "url": "https://www.youtube.com/@channel_name/videos",
        "max_downloads": 100
    }
)
```

### 3. تحميل بجودة محددة (1080p)

```python
response = requests.post(
    "http://localhost:5000/api/download",
    json={
        "url": "VIDEO_URL",
        "format_type": "specific_quality",
        "quality": "1080"
    }
)
```

---

## 🐳 استخدام Docker (اختياري)

```bash
# بناء وتشغيل
docker-compose up -d

# عرض السجلات
docker-compose logs -f

# إيقاف
docker-compose down
```

---

## ⚠️ ملاحظات مهمة

### 1. ffmpeg ضروري
```bash
# تأكد من تثبيت ffmpeg
ffmpeg -version
```

### 2. للفيديوهات المحمية
- احتفظ بملف cookies من متصفحك
- سجل دخول إلى YouTube أولاً
- استخدم إضافة "Get cookies.txt"

### 3. حقوق الاستخدام
- استخدم الأداة بشكل قانوني
- احترم حقوق النشر
- التزم بشروط خدمة YouTube

---

## 🆘 المساعدة والدعم

### إذا واجهت مشكلة:
1. راجع `TROUBLESHOOTING.md` أولاً
2. تأكد من تثبيت جميع المتطلبات
3. تحقق من تحديث yt-dlp:
   ```bash
   pip install --upgrade yt-dlp
   ```

### الأخطاء الشائعة:
- ❌ ffmpeg not found → ثبّت ffmpeg
- ❌ 403 Forbidden → استخدم ملف cookies
- ❌ Age restricted → سجل دخول وصدّر cookies

---

## 📚 الملفات حسب الأولوية

### اقرأ بهذا الترتيب:
1. **00_START_HERE.md** ← أنت هنا!
2. **QUICK_START.md** - ابدأ التشغيل
3. **README_AR.md** - التوثيق الشامل
4. **API_EXAMPLES.md** - أمثلة عملية
5. **ADVANCED_FEATURES.md** - ميزات متقدمة
6. **TROUBLESHOOTING.md** - حل المشاكل

---

## 🎨 الخصائص التقنية

### التقنيات المستخدمة:
- **Python 3.8+** - لغة البرمجة
- **Flask** - إطار الويب
- **yt-dlp** - محرك التحميل
- **ffmpeg** - معالجة الوسائط
- **Docker** - النشر (اختياري)

### معلومات API:
- **المنفذ الافتراضي:** 5000
- **التنسيق:** RESTful JSON API
- **المصادقة:** غير مطلوبة (محلي)
- **CORS:** مفعّل

---

## 🚀 الخطوات التالية

بعد التشغيل الناجح:

1. **جرّب الأمثلة** في `example_usage.py`
2. **استكشف الخيارات** في `ADVANCED_FEATURES.md`
3. **خصص API** حسب احتياجاتك
4. **انشر بـ Docker** للإنتاج
5. **شارك مع الآخرين** إذا أردت!

---

## 💬 تواصل واستفسارات

- 📘 اقرأ التوثيق الكامل في `README_AR.md`
- 🔍 ابحث في `TROUBLESHOOTING.md` للحلول
- 💡 راجع `API_EXAMPLES.md` لأمثلة أكثر
- 🌐 زر [yt-dlp على GitHub](https://github.com/yt-dlp/yt-dlp)

---

## ✅ قائمة التحقق السريعة

- [ ] ثبّت Python 3.8+
- [ ] ثبّت المتطلبات: `pip install -r requirements.txt`
- [ ] ثبّت ffmpeg
- [ ] شغّل API: `python youtube_downloader_api.py`
- [ ] اختبر بـ: `python example_usage.py`
- [ ] احصل على cookies (للمحتوى المحمي)
- [ ] اقرأ التوثيق الكامل

---

**🎉 مبروك! أنت الآن جاهز لتحميل أي فيديو من YouTube!**

**📌 نصيحة نهائية:** ابدأ بالأمثلة البسيطة، ثم انتقل تدريجياً للمميزات المتقدمة.

---

**صُنع بـ ❤️ من أجلك**
