# 🚀 دليل البدء السريع

## التثبيت والتشغيل

### 1. التثبيت العادي

```bash
# تثبيت المتطلبات
pip install -r requirements.txt

# تثبيت ffmpeg
# على Ubuntu/Debian:
sudo apt-get install ffmpeg

# تشغيل API
python youtube_downloader_api.py
```

### 2. استخدام Docker

```bash
# بناء وتشغيل الحاوية
docker-compose up -d

# عرض السجلات
docker-compose logs -f

# إيقاف الحاوية
docker-compose down
```

## الاستخدام السريع

### من خلال Python

```bash
# تشغيل الأمثلة التفاعلية
python example_usage.py
```

### من خلال cURL

```bash
# اختبار API
./test_api.sh
```

### من خلال Postman أو أي أداة HTTP

```
POST http://localhost:5000/api/download
Content-Type: application/json

{
    "url": "https://youtube.com/watch?v=...",
    "format_type": "best",
    "async": true
}
```

## الخيارات الرئيسية

### format_type:
- `best` - أفضل جودة
- `audio` - صوت فقط (MP3)
- `video_audio` - فيديو + صوت
- `specific_quality` - جودة محددة

### quality (عند اختيار specific_quality):
- `2160` - 4K
- `1080` - Full HD
- `720` - HD
- `480` - SD

## نقاط النهاية الرئيسية

- `POST /api/info` - معلومات الفيديو
- `POST /api/download` - تحميل فيديو
- `POST /api/download/playlist` - تحميل قائمة
- `GET /api/status/{id}` - حالة التحميل
- `POST /api/formats` - الصيغ المتاحة

## ملاحظات مهمة

1. **للفيديوهات المحمية عمرياً**: أضف ملف كوكيز من متصفحك
2. **ffmpeg مطلوب**: لدمج الصوت والفيديو
3. **المنفذ الافتراضي**: 5000
4. **مجلد التحميلات**: `./downloads`

## المساعدة

للمزيد من المعلومات، راجع:
- `README_AR.md` - توثيق شامل باللغة العربية
- `example_usage.py` - أمثلة تفصيلية

