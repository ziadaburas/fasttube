# 🎬 YouTube Downloader API

## 📝 الوصف

واجهة برمجية متقدمة لتحميل الفيديوهات من YouTube باستخدام Python و Flask و yt-dlp. يدعم جميع أنواع الفيديوهات بما في ذلك:

- ✅ الفيديوهات العادية
- ✅ الفيديوهات المحمية بالفئة العمرية
- ✅ البث المباشر
- ✅ قوائم التشغيل
- ✅ الفيديوهات المحمية جغرافياً
- ✅ فيديوهات القنوات المميزة
- ✅ تحميل الصوت فقط (MP3)
- ✅ اختيار الجودة المطلوبة

## 🚀 المميزات

### 1. تحميل متقدم
- تحميل بأفضل جودة متاحة
- اختيار جودة محددة (4K, 1080p, 720p, إلخ)
- تحميل الصوت فقط بصيغة MP3
- دعم التحميل غير المتزامن
- متابعة تقدم التحميل في الوقت الفعلي

### 2. تجاوز القيود
- تجاوز القيود العمرية
- تجاوز القيود الجغرافية
- دعم الكوكيز للمحتوى المحمي
- معالجة متقدمة للأخطاء

### 3. دعم القوائم
- تحميل قوائم تشغيل كاملة
- تحديد عدد الفيديوهات المراد تحميلها
- معلومات تفصيلية عن القائمة

### 4. معلومات شاملة
- استخراج كل معلومات الفيديو
- عرض جميع الصيغ المتاحة
- حفظ الترجمات
- حفظ الصور المصغرة والوصف

## 📦 التثبيت

### 1. تثبيت المتطلبات

```bash
# تثبيت Python 3.8 أو أحدث

# تثبيت المكتبات المطلوبة
pip install -r requirements.txt

# تثبيت ffmpeg (مطلوب لدمج الصوت والفيديو)
# على Ubuntu/Debian:
sudo apt-get install ffmpeg

# على macOS:
brew install ffmpeg

# على Windows:
# قم بتحميل ffmpeg من https://ffmpeg.org/download.html
```

### 2. إعداد الكوكيز (اختياري - للمحتوى المحمي عمرياً)

```bash
# إذا كنت بحاجة للوصول إلى محتوى محمي بالفئة العمرية:
# 1. استخدم إضافة "Get cookies.txt" في متصفحك
# 2. قم بتسجيل الدخول إلى YouTube
# 3. احفظ ملف الكوكيز
# 4. حدث مسار الكوكيز في الكود:

# في الدالة get_cookies_for_age_restricted():
return "/path/to/cookies.txt"
```

## 🎯 الاستخدام

### تشغيل API

```bash
python youtube_downloader_api.py
```

سيعمل API على: `http://localhost:5000`

### استخدام أمثلة الكود

```bash
python example_usage.py
```

## 📡 نقاط النهاية (Endpoints)

### 1. الحصول على معلومات الفيديو
```http
POST /api/info
Content-Type: application/json

{
    "url": "https://youtube.com/watch?v=..."
}
```

**الاستجابة:**
```json
{
    "title": "عنوان الفيديو",
    "duration": 180,
    "views": 1000000,
    "likes": 50000,
    "uploader": "اسم القناة",
    "age_limited": false,
    "is_live": false,
    "formats": [...]
}
```

### 2. تحميل فيديو
```http
POST /api/download
Content-Type: application/json

{
    "url": "https://youtube.com/watch?v=...",
    "format_type": "best",
    "quality": "1080",
    "async": true
}
```

**format_type:**
- `best` - أفضل جودة (افتراضي)
- `audio` - صوت فقط (MP3)
- `video_audio` - فيديو وصوت منفصلين
- `specific_quality` - جودة محددة

**quality:**
- `2160` - 4K
- `1440` - 2K
- `1080` - Full HD
- `720` - HD
- `480` - SD
- `360` - Low

**الاستجابة:**
```json
{
    "download_id": "uuid-here",
    "message": "Download started",
    "status_url": "/api/status/uuid-here"
}
```

### 3. تحميل قائمة تشغيل
```http
POST /api/download/playlist
Content-Type: application/json

{
    "url": "https://youtube.com/playlist?list=...",
    "format_type": "best",
    "max_downloads": 50
}
```

### 4. متابعة حالة التحميل
```http
GET /api/status/{download_id}
```

**الاستجابة:**
```json
{
    "status": "downloading",
    "progress": "45.2%",
    "speed": "2.5MB/s",
    "eta": "00:02:15",
    "filename": "video.mp4",
    "downloaded": "150MB",
    "total": "330MB"
}
```

### 5. عرض جميع الصيغ المتاحة
```http
POST /api/formats
Content-Type: application/json

{
    "url": "https://youtube.com/watch?v=..."
}
```

### 6. فحص صحة API
```http
GET /api/health
```

## 💻 أمثلة على الاستخدام بلغة Python

### مثال 1: الحصول على معلومات فيديو

```python
import requests

url = "https://youtube.com/watch?v=dQw4w9WgXcQ"
response = requests.post(
    "http://localhost:5000/api/info",
    json={"url": url}
)

info = response.json()
print(f"العنوان: {info['title']}")
print(f"المدة: {info['duration']} ثانية")
```

### مثال 2: تحميل فيديو بأفضل جودة

```python
import requests
import time

url = "https://youtube.com/watch?v=dQw4w9WgXcQ"

# بدء التحميل
response = requests.post(
    "http://localhost:5000/api/download",
    json={
        "url": url,
        "format_type": "best",
        "async": True
    }
)

download_id = response.json()['download_id']

# متابعة التقدم
while True:
    status_response = requests.get(
        f"http://localhost:5000/api/status/{download_id}"
    )
    status = status_response.json()
    
    if status['status'] == 'completed':
        print("اكتمل التحميل!")
        break
    
    print(f"التقدم: {status['progress']}")
    time.sleep(2)
```

### مثال 3: تحميل صوت فقط

```python
import requests

url = "https://youtube.com/watch?v=dQw4w9WgXcQ"
response = requests.post(
    "http://localhost:5000/api/download",
    json={
        "url": url,
        "format_type": "audio",
        "async": True
    }
)

print(response.json())
```

### مثال 4: تحميل قائمة تشغيل

```python
import requests

playlist_url = "https://youtube.com/playlist?list=..."
response = requests.post(
    "http://localhost:5000/api/download/playlist",
    json={
        "url": playlist_url,
        "format_type": "best",
        "max_downloads": 10
    }
)

print(response.json())
```

## 🔧 التخصيص المتقدم

### إضافة الكوكيز للمحتوى المحمي

في ملف `youtube_downloader_api.py`، عدّل الدالة:

```python
def get_cookies_for_age_restricted():
    # أضف مسار ملف الكوكيز الخاص بك
    return "/path/to/your/cookies.txt"
```

### تغيير مجلد التحميلات

```python
DOWNLOAD_DIR = Path("/your/custom/path")
```

### تخصيص إعدادات yt-dlp

عدّل الدالة `get_ydl_opts()` لإضافة خيارات إضافية:

```python
ydl_opts.update({
    'ratelimit': 1000000,  # حد السرعة بالبايت/ثانية
    'sleep_interval': 2,    # وقت الانتظار بين الطلبات
    # المزيد من الخيارات...
})
```

## ⚙️ الخيارات المتقدمة لـ yt-dlp

API يدعم العديد من الخيارات المتقدمة:

- **تجاوز القيود الجغرافية**: `geo_bypass: True`
- **تحميل الترجمات**: `writesubtitles: True`
- **حفظ الصور المصغرة**: `writethumbnail: True`
- **دعم البث المباشر**: `live_from_start: True`
- **إعادة المحاولة**: `retries: 10`
- **تحميل متعدد الأجزاء**: `concurrent_fragment_downloads: 5`

## 🐛 معالجة الأخطاء

جميع نقاط النهاية تعيد استجابات JSON مع معلومات الخطأ:

```json
{
    "error": "وصف الخطأ هنا",
    "status": "error"
}
```

### الأخطاء الشائعة وحلولها:

1. **خطأ 403 Forbidden**
   - الحل: أضف ملف كوكيز صالح

2. **خطأ في التحقق من العمر**
   - الحل: استخدم كوكيز من حساب YouTube مسجل دخول

3. **الفيديو غير متاح**
   - الحل: تحقق من القيود الجغرافية

4. **خطأ في ffmpeg**
   - الحل: تأكد من تثبيت ffmpeg بشكل صحيح

## 📊 الأداء والتحسين

- **التحميل غير المتزامن**: يسمح بتحميل عدة فيديوهات في نفس الوقت
- **معالجة متعددة الخيوط**: لا تتأثر بعمليات التحميل الطويلة
- **حد السرعة**: يمكن تحديد حد أقصى للسرعة
- **استئناف التحميل**: يدعم استئناف التحميلات المتوقفة

## 🔐 الأمان والخصوصية

- لا يتم تخزين كلمات المرور
- الكوكيز تبقى محلية
- لا يتم إرسال البيانات لأطراف ثالثة
- يمكن حذف الملفات المحملة تلقائياً

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتعليمي.

## ⚠️ تنويه قانوني

يرجى احترام حقوق النشر والملكية الفكرية:
- استخدم هذه الأداة فقط للمحتوى الذي لديك الحق في تحميله
- التزم بشروط خدمة YouTube
- لا تستخدم الأداة لأغراض تجارية غير مصرح بها

## 🤝 المساهمة

المساهمات مرحب بها! يمكنك:
- الإبلاغ عن الأخطاء
- اقتراح ميزات جديدة
- تحسين الكود
- تحديث التوثيق

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من ملف README
2. راجع الأمثلة في `example_usage.py`
3. تأكد من تثبيت جميع المتطلبات
4. تحقق من إصدار yt-dlp (قد تحتاج للتحديث)

## 🔄 التحديثات

لتحديث yt-dlp لأحدث إصدار:

```bash
pip install --upgrade yt-dlp
```

## 📚 مصادر إضافية

- [توثيق yt-dlp](https://github.com/yt-dlp/yt-dlp)
- [توثيق Flask](https://flask.palletsprojects.com/)
- [توثيق FFmpeg](https://ffmpeg.org/documentation.html)

---

**صُنع بـ ❤️ للمجتمع العربي**
